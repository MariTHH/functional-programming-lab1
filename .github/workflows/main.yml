name: .NET Core CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore FsSolution/FsSolution.fsproj

    - name: Build the project
      run: dotnet build FsSolution/FsSolution.fsproj --configuration Debug

    - name: Run tests
      run: dotnet test Tests/Tests.fsproj --verbosity normal
